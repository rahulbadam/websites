"use client"; import { useState } from "react"; import Link from "next/link"; import { motion, AnimatePresence } from "framer-motion"; import { Menu, X } from "lucide-react"; import { cn } from "@websites/utils";
const navLinks = [{ href: "#about", label: "About" }, { href: "#programs", label: "Programs" }, { href: "#impact", label: "Impact" }, { href: "#stories", label: "Stories" }, { href: "#donate", label: "Donate" }];
export function Navbar() { const [isOpen, setIsOpen] = useState(false); return (<motion.header initial={{ y: -100 }} animate={{ y: 0 }} className="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-xl shadow-sm"><nav className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><Link href="/" className="text-xl font-bold text-primary">Akshara Foundation</Link><div className="hidden md:flex items-center gap-8">{navLinks.map(l => <a key={l.href} href={l.href} className="text-sm font-medium text-muted-foreground hover:text-primary">{l.label}</a>)}</div><button onClick={() => setIsOpen(!isOpen)} className="md:hidden p-2"><motion.div animate={{ rotate: isOpen ? 90 : 0 }}>{isOpen ? <X /> : <Menu />}</motion.div></button></nav><AnimatePresence>{isOpen && (<motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} className="md:hidden border-t bg-white px-6 py-4 space-y-3">{navLinks.map(l => <a key={l.href} href={l.href} onClick={() => setIsOpen(false)} className="block text-base font-medium">{l.label}</a>)}</motion.div>)}</AnimatePresence></motion.header>); }