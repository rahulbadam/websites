"use client"; import { motion } from "framer-motion"; import { useInView } from "@websites/hooks"; import { BookOpen, School, Users, Award } from "lucide-react";
const stats = [{ icon: BookOpen, value: "50,000+", label: "Students Educated" }, { icon: School, value: "1,000+", label: "Partner Schools" }, { icon: Users, value: "100+", label: "Active Volunteers" }, { icon: Award, value: "10+", label: "Years of Service" }];
export function Impact() { const { ref, isInView } = useInView({ threshold: 0.3 }); return (<section id="impact" className="py-20 bg-white"><div ref={ref} className="mx-auto max-w-7xl px-6 lg:px-8"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-16"><h2 className="text-3xl font-bold text-slate-900">Our Impact</h2><p className="mt-4 text-lg text-slate-600">Measurable change in communities across India</p></motion.div><div className="grid grid-cols-2 lg:grid-cols-4 gap-8">{stats.map((s, i) => (<motion.div key={s.label} initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ delay: i * 0.1 }} className="text-center p-6 rounded-2xl bg-slate-50"><s.icon className="h-8 w-8 mx-auto text-blue-600 mb-4" /><div className="text-3xl font-bold text-slate-900">{s.value}</div><div className="mt-1 text-sm text-slate-500">{s.label}</div></motion.div>))}</div></div></section>); }