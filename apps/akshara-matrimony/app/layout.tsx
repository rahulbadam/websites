import type { Metadata } from "next"; import { Inter } from "next/font/google"; import "./globals.css";
const inter = Inter({ subsets: ["latin"], variable: "--font-sans", display: "swap" });
export const metadata: Metadata = { title: "Akshara Matrimony | Find Your Perfect Match", description: "Premium matrimonial service with 10,000+ verified profiles. Trusted by families across India for meaningful connections.", metadataBase: new URL("https://aksharamatrimony.com") };
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body className={`${inter.variable} font-sans`}>{children}</body></html>; }