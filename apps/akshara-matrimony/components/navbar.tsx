"use client"; import { useState } from "react"; import Link from "next/link"; import { motion, AnimatePresence } from "framer-motion"; import { Menu, X, Crown } from "lucide-react";
const navLinks = [{ href: "#about", label: "Home" }, { href: "#profiles", label: "Browse" }, { href: "#success-stories", label: "Stories" }, { href: "#plans", label: "Plans" }];
export function Navbar() { const [isOpen, setIsOpen] = useState(false); return (<motion.header initial={{ y: -100 }} animate={{ y: 0 }} className="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-xl shadow-sm"><nav className="mx-auto flex max-w-7xl items-center justify-between px-6 py-3"><Link href="/" className="flex items-center gap-2 text-lg font-bold text-rose-700"><Crown className="h-5 w-5" /> Akshara Matrimony</Link><div className="hidden md:flex items-center gap-6">{navLinks.map(l => <a key={l.href} href={l.href} className="text-sm text-slate-600 hover:text-rose-700">{l.label}</a>)}<button className="bg-rose-600 text-white px-5 py-2 rounded-full text-sm font-medium hover:bg-rose-700">Join Free</button></div><button onClick={() => setIsOpen(!isOpen)} className="md:hidden p-2">{isOpen ? <X /> : <Menu />}</button></nav><AnimatePresence>{isOpen && (<motion.div initial={{ height: 0 }} animate={{ height: "auto" }} exit={{ height: 0 }} className="md:hidden border-t bg-white px-6 py-4 space-y-3">{navLinks.map(l => <a key={l.href} href={l.href} className="block text-base">{l.label}</a>)}</motion.div>)}</AnimatePresence></motion.header>); }