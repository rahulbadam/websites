"use client"; import { useState } from "react"; import Link from "next/link"; import { motion, AnimatePresence } from "framer-motion"; import { Menu, X, Phone } from "lucide-react";
const navLinks = [{ href: "#about", label: "About" }, { href: "#departments", label: "Departments" }, { href: "#doctors", label: "Doctors" }, { href: "#emergency", label: "Emergency" }];
export function Navbar() { const [isOpen, setIsOpen] = useState(false); return (<motion.header initial={{ y: -100 }} animate={{ y: 0 }} className="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-xl shadow-sm border-b border-slate-100"><nav className="mx-auto flex max-w-7xl items-center justify-between px-6 py-3"><Link href="/" className="text-lg font-bold text-teal-700">Vijaya Hospital</Link><div className="hidden md:flex items-center gap-6">{navLinks.map(l => <a key={l.href} href={l.href} className="text-sm text-slate-600 hover:text-teal-700">{l.label}</a>)}<a href="tel:+919876543210" className="flex items-center gap-1.5 rounded-full bg-teal-600 px-4 py-2 text-sm font-medium text-white hover:bg-teal-700"><Phone className="h-3.5 w-3.5" /> Emergency</a></div><button onClick={() => setIsOpen(!isOpen)} className="md:hidden p-2">{isOpen ? <X /> : <Menu />}</button></nav><AnimatePresence>{isOpen && (<motion.div initial={{ height: 0 }} animate={{ height: "auto" }} exit={{ height: 0 }} className="md:hidden border-t bg-white px-6 py-4 space-y-3">{navLinks.map(l => <a key={l.href} href={l.href} className="block text-base">{l.label}</a>)}</motion.div>)}</AnimatePresence></motion.header>); }