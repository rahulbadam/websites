"use client"; import { motion } from "framer-motion"; import { Star } from "lucide-react";
const doctors = [{ name: "Dr. Ramesh Kumar", spec: "Chief Cardiologist", exp: "25+ years", rating: 4.9 }, { name: "Dr. Priya Sharma", spec: "Senior Neurologist", exp: "18+ years", rating: 4.8 }, { name: "Dr. Anand Reddy", spec: "Orthopedic Surgeon", exp: "20+ years", rating: 4.9 }, { name: "Dr. Sunita Devi", spec: "Head of Pediatrics", exp: "15+ years", rating: 4.7 }];
export function Doctors() { return (<section id="doctors" className="py-20 bg-white"><div className="mx-auto max-w-7xl px-6 lg:px-8"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-16"><h2 className="text-3xl font-bold text-slate-900">Meet Our Doctors</h2><p className="mt-4 text-lg text-slate-600">Experienced specialists dedicated to your care</p></motion.div><div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">{doctors.map((d, i) => (<motion.div key={d.name} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className="text-center p-6 rounded-2xl border hover:shadow-lg transition-all"><div className="h-20 w-20 mx-auto rounded-full bg-teal-100 flex items-center justify-center text-teal-700 text-xl font-bold mb-4">{d.name.split(" ").map(n=>n[0]).join("")}</div><h3 className="font-semibold text-slate-900">{d.name}</h3><p className="text-teal-600 text-sm">{d.spec}</p><p className="text-slate-500 text-sm mt-1">{d.exp}</p><div className="flex items-center justify-center gap-1 mt-3"><Star className="h-4 w-4 fill-amber-400 text-amber-400" /><span className="text-sm font-medium">{d.rating}</span></div></motion.div>))}</div></div></section>); }