"use client"; import { motion } from "framer-motion"; import { Check } from "lucide-react";
const plans = [{ name: "Free", price: "₹0", period: "forever", features: ["Create profile", "Browse matches", "Limited messages"], highlighted: false }, { name: "Gold", price: "₹999", period: "month", features: ["Unlimited messages", "Priority support", "View contact details", "Advanced filters"], highlighted: true }, { name: "Diamond", price: "₹2,499", period: "month", features: ["Personal matchmaker", "Video calls", "Profile highlighting", "Relationship coach"], highlighted: false }];
export function Plans() { return (<section id="plans" className="py-20 bg-slate-50"><div className="mx-auto max-w-7xl px-6 lg:px-8"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-16"><h2 className="text-3xl font-bold text-slate-900">Membership Plans</h2><p className="mt-4 text-lg text-slate-600">Choose the plan that's right for you</p></motion.div><div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">{plans.map((p, i) => (<motion.div key={p.name} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className={`rounded-2xl p-8 ${p.highlighted ? "bg-rose-600 text-white shadow-xl scale-105" : "bg-white shadow-sm"}`}><h3 className="text-xl font-bold mb-2">{p.name}</h3><div className="flex items-baseline gap-1 mb-6"><span className="text-3xl font-bold">{p.price}</span><span className={`text-sm ${p.highlighted ? "text-rose-100" : "text-slate-500"}`}>/{p.period}</span></div><ul className="space-y-3 mb-8">{p.features.map(f => (<li key={f} className="flex items-center gap-2 text-sm"><Check className={`h-4 w-4 ${p.highlighted ? "text-rose-200" : "text-green-500"}`} /> {f}</li>))}</ul><button className={`w-full py-3 rounded-full font-semibold text-sm ${p.highlighted ? "bg-white text-rose-600 hover:bg-rose-50" : "bg-rose-600 text-white hover:bg-rose-700"} transition-colors`}>Get Started</button></motion.div>))}</div></div></section>); }