"use client"; import { motion } from "framer-motion"; import { useInView } from "@websites/hooks"; import { Users, Heart, Star, CheckCircle } from "lucide-react";
const stats = [{ icon: Users, value: "10,000+", label: "Active Users" }, { icon: Heart, value: "1,000+", label: "Success Stories" }, { icon: Star, value: "95%", label: "Satisfaction Rate" }, { icon: CheckCircle, value: "100%", label: "Verified Profiles" }];
export function Stats() { const { ref, isInView } = useInView({ threshold: 0.3 }); return (<section className="py-16 bg-white border-y"><div ref={ref} className="mx-auto max-w-7xl px-6 lg:px-8"><div className="grid grid-cols-2 lg:grid-cols-4 gap-8">{stats.map((s, i) => (<motion.div key={s.label} initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ delay: i * 0.1 }} className="text-center p-4"><s.icon className="h-8 w-8 mx-auto text-rose-500 mb-3" /><div className="text-3xl font-bold text-slate-900">{s.value}</div><div className="text-sm text-slate-500">{s.label}</div></motion.div>))}</div></div></section>); }