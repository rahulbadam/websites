"use client"; import { motion } from "framer-motion"; import { MapPin, Briefcase, GraduationCap, Verified } from "lucide-react";
const profiles = [{ name: "Ananya R.", age: 26, edu: "M.Tech", work: "Software Engineer", loc: "Hyderabad", match: 95 }, { name: "Vikram S.", age: 28, edu: "MBA", work: "Product Manager", loc: "Bangalore", match: 92 }, { name: "Priya M.", age: 25, edu: "MBBS", work: "Doctor", loc: "Chennai", match: 88 }, { name: "Rahul K.", age: 27, edu: "CA", work: "Chartered Accountant", loc: "Mumbai", match: 90 }];
export function Profiles() { return (<section id="profiles" className="py-20 bg-slate-50"><div className="mx-auto max-w-7xl px-6 lg:px-8"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-16"><h2 className="text-3xl font-bold text-slate-900">Featured Profiles</h2><p className="mt-4 text-lg text-slate-600">Handpicked verified profiles for you</p></motion.div><div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">{profiles.map((p, i) => (<motion.div key={p.name} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} whileHover={{ y: -5 }} className="bg-white rounded-2xl p-6 shadow-sm hover:shadow-md transition-all text-center"><div className="h-16 w-16 mx-auto rounded-full bg-rose-100 flex items-center justify-center text-rose-600 text-xl font-bold mb-4">{p.name[0]}</div><div className="flex items-center justify-center gap-1 mb-2"><h3 className="font-semibold text-slate-900">{p.name}</h3><Verified className="h-4 w-4 text-blue-500" /></div><p className="text-sm text-slate-500 mb-3">{p.age} yrs • {p.loc}</p><div className="space-y-1 text-sm text-slate-600"><div className="flex items-center justify-center gap-1"><GraduationCap className="h-3.5 w-3.5" /> {p.edu}</div><div className="flex items-center justify-center gap-1"><Briefcase className="h-3.5 w-3.5" /> {p.work}</div></div><div className="mt-4 inline-flex items-center rounded-full bg-green-50 px-3 py-1 text-xs font-medium text-green-700">{p.match}% Match</div></motion.div>))}</div></div></section>); }