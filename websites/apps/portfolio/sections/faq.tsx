"use client"; import { useState } from "react"; import { motion, AnimatePresence } from "framer-motion"; import { ChevronDown } from "lucide-react";
const faqs = [{ q: "What is your typical project timeline?", a: "Most projects take 4-12 weeks depending on complexity. I provide detailed timelines during discovery." }, { q: "Do you work with international clients?", a: "Yes, I work with clients globally. My schedule accommodates multiple time zones." }, { q: "What technologies do you specialize in?", a: "React, Next.js, TypeScript, Node.js, PostgreSQL, GraphQL, and AWS. I adapt to your stack." }, { q: "How do you handle communication?", a: "Weekly video calls, daily Slack updates, and access to project management tools." }, { q: "What is your pricing model?", a: "Project-based or retainer. Transparent pricing with no hidden costs." }];
export function FAQ() { const [open, setOpen] = useState<number | null>(0); return (<section id="faq" className="relative bg-[#09090b] py-32 overflow-hidden"><div className="absolute inset-0 bg-gradient-to-b from-transparent via-purple-600/[0.02] to-transparent" /><div className="relative mx-auto max-w-3xl px-6 lg:px-8"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center"><h2 className="text-3xl font-bold tracking-tight text-white sm:text-5xl">FAQ</h2><p className="mt-4 text-lg text-zinc-500">Common questions answered</p></motion.div><div className="mt-16 space-y-3">{faqs.map((f, i) => (<motion.div key={i} initial={{ opacity: 0, y: 10 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.05 }} className="rounded-xl border border-white/5 bg-white/[0.02] overflow-hidden"><button onClick={() => setOpen(open === i ? null : i)} className="flex w-full items-center justify-between p-6 text-left"><span className="font-medium text-zinc-300">{f.q}</span><ChevronDown className={`h-5 w-5 text-zinc-600 transition-transform ${open === i ? "rotate-180" : ""}`} /></button><AnimatePresence>{open === i && (<motion.div initial={{ height: 0 }} animate={{ height: "auto" }} exit={{ height: 0 }} className="overflow-hidden"><p className="px-6 pb-6 text-zinc-400">{f.a}</p></motion.div>)}</AnimatePresence></motion.div>))}</div></div></section>); }